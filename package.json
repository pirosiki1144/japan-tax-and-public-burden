{
  "name": "japan-tax-and-public-burden",
  "version": "0.1.0",
  "private": true,
  "description": "Structured data for taxes and public burdens in Japan",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate/validate-data.js"
  },
  "engines": {
    "node": ">=20"
  }
}
