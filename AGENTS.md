# AGENTS.md

## 基本方針

- 公式一次情報を優先し、事実には `source_url` と確認日時を付ける。
- 巡回URLは `config/sources.yaml` だけに置き、プログラムへ直書きしない。
- 税金等の主状態と法律手続の状態を分離する。
- 公布日、施行日、適用開始日、徴収開始日を個別に扱い、推測で補完しない。
- 和暦等の原文と正規化値を併記する。不明値は `unknown` と根拠不足を記録する。
- 段階適用は複数のphaseとして保存する。
- 変更前に既存の `tax_id`、`change_id`、Issueとの重複を検索する。
- ニュースで検知した未確認事項はIssue、公式根拠に基づくデータ変更はPull Requestで扱う。
- 金額の確定区分、期間、単位、集計範囲を保存し、内数を二重計上しない。
- Issueの自動削除、Pull Requestの自動マージは行わない。
- 成立を公式情報で確認したIssueは、変更データと根拠を保存して成立済みラベルを付け、Closeして監査履歴を残す。
- `.yaml` はYAML 1.2として記述し、JSON互換記法と一般的なYAML記法の両方を許可する。
- 自動化スクリプトは当面Node.jsのJavaScript（ES Modules）で実装する。TypeScriptへの移行は必要性と移行範囲を別途レビューする。

## 変更手順

1. 対応するGitHub Issueを確認する。Issueがなければ、実装前に目的、対象範囲、受け入れ条件を記載したIssueを作成する。
2. Issueの範囲と受け入れ条件を作業の基準とし、範囲を超える変更は同じIssueへ追記するか別Issueへ分ける。
3. `PROJECT_SPEC.md` と、データ変更の場合は対象schemaを読む。
4. 事実やデータを変更する場合は、根拠となる公式URLを確認する。
5. データ変更の場合は既存IDと重複を検索する。
6. Issueの受け入れ条件を満たす最小限の変更を行う。
7. `npm ci` 後に `npm test` と `npm run validate` を実行する。
8. PR本文に関連Issue、受け入れ条件の結果、根拠URL、旧値、新値、影響範囲、不確実な点を記載する。
9. ユーザーによるPRレビューを実装内容に対する確認として扱う。エージェントはPRをマージせず、人間の判断と実行を待つ。

## Issue単位の開発承認

ユーザーがIssue番号を指定して実装を依頼した場合、またはIssueに `status:ready` ラベルが付与された場合、そのIssueの目的、対象範囲、受け入れ条件に対する開発承認が得られたものとする。Issueを作成しただけでは着手承認とみなさない。

承認後は、Issueの範囲内に限り、調査、読み取り、編集、依存関係の導入、テスト、検証、作業ブランチの作成、commit、push、Issue・PRの参照と更新、`main`向けドラフトPRの作成、CI確認、CI失敗やレビュー指摘への修正を、操作ごとの追加承認なしで継続する。人間がPRをレビューし、マージ可否を判断できる状態で停止する。

`develop`ブランチは設けず、`main`を正本およびデフォルトブランチとする。通常はIssueごとの作業ブランチから`main`へPRを作成する。`main`への直接pushは、対象変更についてユーザーが明示的に指示した場合だけ実行する。

## エージェントの役割分担

- 主担当はIssueの範囲管理、実装、統合、ユーザーへの報告に責任を持つ。
- 通常のIssueは実装を1担当に集約し、役割分担による重複調査や過剰なトークン消費を避ける。
- 外部一次情報、schema、権限、CIなどの独立検証が必要な高リスクIssueでのみ、調査担当や検証担当を追加する。
- 依存関係があるIssueは無理に並列実行しない。
- 各担当には、対象Issue、関連仕様、対象ファイル、直前工程の成果だけを渡す。

## 停止と追加承認

次の操作や状態では作業を停止し、必要な権限と影響を示してユーザーの指示を求める。

- PRのマージ、Issueの削除、force push、履歴改変、広範囲な削除。
- secrets、リポジトリや外部サービスの権限、課金、公開範囲、第三者への実質的な通知を変更する操作。
- Issueの目的や対象範囲を実質的に変更する作業。
- 公式情報の不一致や根拠不足により、推測によるデータ変更が必要な状態。
- 未決定のライセンスを表記する操作。
- ユーザーの明示指示がない`main`への直接push。

取得失敗、参照元の構造変更、ソース間の不一致を黙って無視しない。データを推測で直さず、必要に応じてIssueを作成する。
